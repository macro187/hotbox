#!/bin/sh
set -eu
HOTBOX=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)


. $HOTBOX/lib/sh.sh


cook="$(which cook)"
test -n "$cook" || exit 0


cookdir="${cook%/*}"


echo --volume $cookdir:/cook
